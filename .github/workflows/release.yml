on:
  push:
  workflow_dispatch:
  release:
    types:
      - published

name: Update release

jobs:
  update:
    name: update
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Auto-PR
        run: |
          iex "& {$(irm get.scoop.sh)} -RunAsAdmin"
          scoop install hub
          ./bin/auto-pr.ps1 -r
